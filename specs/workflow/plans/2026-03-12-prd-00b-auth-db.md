# PRD-00b — Auth + DB: Implementation Plan

## Overview

Implementar a infraestrutura de banco de dados (Prisma schema com 12 entidades) e autenticacao (Supabase Auth com OAuth Google), incluindo protecao de rotas via `proxy.ts`, layout autenticado com `SessionProvider`, e todos os arquivos de `lib/`. Ao final, `pnpm build` passa e rotas internas redirecionam para login quando nao autenticado.

## Current State Analysis

O PRD-00a esta completo. Existem hoje:

- `src/app/layout.tsx` — layout raiz com Inter + Geist_Mono
- `src/app/page.tsx` — placeholder "Setup funcionando"
- `src/app/globals.css` — Tailwind v4 + shadcn tema OKLCH
- `src/lib/utils.ts` — `cn()` helper
- `src/components/ui/button.tsx` — componente shadcn
- Pastas vazias com `.gitkeep`: `actions/`, `queries/`, `services/`, `types/schemas/`
- `proxy.ts` **nao existe** ainda
- Nenhuma dependencia de Prisma, Supabase ou `server-only` instalada
- `package.json` script `build` e apenas `next build` (precisa de `prisma generate` antes)

## Desired End State

Apos execucao completa deste plano:

1. `prisma/schema.prisma` com 12 modelos, 5 enums, relacoes e cascatas
2. `npx prisma generate` e `npx prisma migrate dev --name init` executam sem erros
3. `pnpm build` passa com zero erros TypeScript
4. Acessar `/dashboard` sem sessao redireciona para `/login`
5. Acessar `/login` exibe pagina com botao "Entrar com Google"
6. Fluxo OAuth completo funciona: login → callback → dashboard com email visivel
7. Acessar `/` redireciona auth-aware (autenticado → `/dashboard`, anonimo → `/login`)
8. `proxy.ts` exporta `proxy()` com `config.matcher`
9. `lib/db.ts` usa `server-only` + singleton `globalThis`
10. `lib/auth.ts` exporta `requireAuth` como `const` com `React.cache()`

### Verificacao:

```bash
npx prisma generate && pnpm build   # zero erros
npx prisma studio                    # 12 tabelas visiveis
```

Manual: fluxo OAuth, redirects, pagina de dashboard com email.

## What We're NOT Doing

- CRUD de nenhuma entidade (PRD-01+)
- Queries, Actions ou Services de dominio
- Schemas Zod para entidades
- Componentes de UI de dominio
- Deploy / `vercel.json` (PRD-09)
- Dashboard real (apenas placeholder)
- Portal publico (PRD-08a)
- Campo `slug` no modelo Projeto (PRD posterior)

## Desvios do PRD-00b

| PRD-00b diz | Plano segue | Motivo |
|---|---|---|
| `src/app/login/page.tsx` (rota direta) | `src/app/(auth)/login/page.tsx` (route group) | `05_urls.md` define `/login` em route group `(auth)` — isola layout de auth do painel |
| Modificar `app/page.tsx` com `redirect('/dashboard')` | Deletar `app/page.tsx`; redirect via `proxy.ts` | `05_urls.md` define `/` como redirect auth-aware via `proxy.ts`, sem `page.tsx` |

## Implementation Approach

3 fases incrementais, cada uma verificavel independentemente:

1. **Dependencias + Schema Prisma** — instala pacotes, cria schema, gera client, roda migration
2. **Infraestrutura lib/** — `db.ts`, `supabase.ts`, `auth.ts`
3. **Rotas e Auth** — `proxy.ts`, callback, login, layout autenticado, dashboard placeholder

---

## Phase 1: Dependencies + Prisma Schema

### Overview

Instalar dependencias, criar o schema Prisma completo com 12 entidades e 5 enums, gerar o client e rodar a migration inicial. Atualizar `package.json` build script.

### Changes Required:

#### 1. Install dependencies

```bash
pnpm add @prisma/client@5 @supabase/ssr @supabase/supabase-js server-only
pnpm add -D prisma@5
```

> Prisma pinado em 5.x (nao 6.x/7.x). `@prisma/client` e `prisma` devem ter a mesma major version.

#### 2. Update build script

**File**: `package.json`
**Changes**: Adicionar `prisma generate` antes de `next build`

```json
"build": "prisma generate && next build"
```

#### 3. Create Prisma schema

**File**: `prisma/schema.prisma`
**Changes**: Schema completo com datasource, generator, 5 enums e 12 modelos

```prisma
datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

generator client {
  provider = "prisma-client-js"
}

// === ENUMS ===

enum StatusProjeto {
  rascunho
  ativo
  aguardando_cliente
  pausado
  concluido
  arquivado
}

enum StatusFase {
  nao_iniciada
  em_andamento
  aguardando_cliente
  concluida
  pausada
  cancelada
}

enum StatusTarefa {
  nao_iniciada
  em_andamento
  concluida
  cancelada
}

enum StatusPendencia {
  aberta
  em_andamento
  resolvida
  cancelada
}

enum TipoEventoTimeline {
  projeto_criado
  fase_iniciada
  fase_concluida
  reuniao_registrada
  decisao_registrada
  checkpoint_registrado
  pendencia_criada
  pendencia_resolvida
  documento_publicado
  mudanca_direcao
  atualizacao_manual
}

// === MODELS ===

model Cliente {
  id                   String    @id @default(uuid()) @db.Uuid
  nome                 String
  empresa_organizacao  String?
  email_principal      String?
  telefone_contato     String?
  observacoes          String?   @db.Text
  created_at           DateTime  @default(now())
  updated_at           DateTime  @updatedAt

  projetos Projeto[]

  @@map("clientes")
}

model Projeto {
  id                  String         @id @default(uuid()) @db.Uuid
  cliente_id          String         @db.Uuid
  nome                String
  descricao           String?        @db.Text
  status              StatusProjeto
  data_inicio         DateTime       @db.Date
  previsao_entrega    DateTime?      @db.Date
  data_conclusao_real DateTime?      @db.Date
  fase_atual_id       String?        @db.Uuid
  created_at          DateTime       @default(now())
  updated_at          DateTime       @updatedAt

  cliente             Cliente        @relation(fields: [cliente_id], references: [id], onDelete: Cascade)
  fases               Fase[]
  lancamentos_horas   LancamentoHoras[]
  reunioes            Reuniao[]
  decisoes            Decisao[]
  checkpoints         Checkpoint[]
  pendencias          Pendencia[]
  documentos          Documento[]
  mudancas_direcao    MudancaDirecao[]
  eventos_timeline    EventoTimeline[]

  @@index([cliente_id])
  @@map("projetos")
}

model Fase {
  id                    String      @id @default(uuid()) @db.Uuid
  projeto_id            String      @db.Uuid
  nome                  String
  descricao             String?     @db.Text
  ordem                 Int
  status                StatusFase
  data_inicio_prevista  DateTime?   @db.Date
  data_fim_prevista     DateTime?   @db.Date
  data_inicio_real      DateTime?   @db.Date
  data_fim_real         DateTime?   @db.Date
  is_fase_geral         Boolean
  created_at            DateTime    @default(now())
  updated_at            DateTime    @updatedAt

  projeto               Projeto     @relation(fields: [projeto_id], references: [id], onDelete: Cascade)
  tarefas               TarefaPlanejada[]
  lancamentos_horas     LancamentoHoras[]
  reunioes              Reuniao[]
  decisoes              Decisao[]
  checkpoints           Checkpoint[]
  pendencias            Pendencia[]
  documentos            Documento[]
  mudancas_direcao      MudancaDirecao[]
  eventos_timeline      EventoTimeline[]

  @@index([projeto_id])
  @@map("fases")
}

model TarefaPlanejada {
  id                    String       @id @default(uuid()) @db.Uuid
  fase_id               String       @db.Uuid
  titulo                String
  descricao             String?      @db.Text
  tempo_estimado_horas  Decimal?     @db.Decimal(8, 2)
  ordem                 Int
  status                StatusTarefa
  created_at            DateTime     @default(now())
  updated_at            DateTime     @updatedAt

  fase                  Fase         @relation(fields: [fase_id], references: [id], onDelete: Cascade)

  @@index([fase_id])
  @@map("tarefas_planejadas")
}

model LancamentoHoras {
  id                String    @id @default(uuid()) @db.Uuid
  projeto_id        String    @db.Uuid
  fase_id           String    @db.Uuid
  data_lancamento   DateTime  @db.Date
  descricao         String    @db.Text
  horas_gastas      Decimal   @db.Decimal(8, 2)
  created_at        DateTime  @default(now())
  updated_at        DateTime  @updatedAt

  projeto           Projeto   @relation(fields: [projeto_id], references: [id], onDelete: Cascade)
  fase              Fase      @relation(fields: [fase_id], references: [id], onDelete: Cascade)

  @@index([projeto_id])
  @@index([fase_id])
  @@map("lancamentos_horas")
}

model Reuniao {
  id                 String    @id @default(uuid()) @db.Uuid
  projeto_id         String    @db.Uuid
  fase_id            String?   @db.Uuid
  titulo             String
  data_reuniao       DateTime  @db.Date
  participantes      String?   @db.Text
  link_referencia    String?
  resumo_executivo   String?   @db.Text
  ata_resumida       String?   @db.Text
  created_at         DateTime  @default(now())
  updated_at         DateTime  @updatedAt

  projeto            Projeto   @relation(fields: [projeto_id], references: [id], onDelete: Cascade)
  fase               Fase?     @relation(fields: [fase_id], references: [id], onDelete: SetNull)
  decisoes           Decisao[]

  @@index([projeto_id])
  @@index([fase_id])
  @@map("reunioes")
}

model Decisao {
  id              String    @id @default(uuid()) @db.Uuid
  projeto_id      String    @db.Uuid
  fase_id         String?   @db.Uuid
  reuniao_id      String?   @db.Uuid
  titulo          String
  descricao       String    @db.Text
  contexto        String?   @db.Text
  impacto         String?   @db.Text
  data_decisao    DateTime  @db.Date
  created_at      DateTime  @default(now())
  updated_at      DateTime  @updatedAt

  projeto         Projeto   @relation(fields: [projeto_id], references: [id], onDelete: Cascade)
  fase            Fase?     @relation(fields: [fase_id], references: [id], onDelete: SetNull)
  reuniao         Reuniao?  @relation(fields: [reuniao_id], references: [id], onDelete: SetNull)

  @@index([projeto_id])
  @@index([fase_id])
  @@index([reuniao_id])
  @@map("decisoes")
}

model Checkpoint {
  id                 String    @id @default(uuid()) @db.Uuid
  projeto_id         String    @db.Uuid
  fase_id            String?   @db.Uuid
  titulo             String
  resumo             String    @db.Text
  proximos_passos    String?   @db.Text
  data_checkpoint    DateTime  @db.Date
  created_at         DateTime  @default(now())
  updated_at         DateTime  @updatedAt

  projeto            Projeto   @relation(fields: [projeto_id], references: [id], onDelete: Cascade)
  fase               Fase?     @relation(fields: [fase_id], references: [id], onDelete: SetNull)

  @@index([projeto_id])
  @@index([fase_id])
  @@map("checkpoints")
}

model Pendencia {
  id              String           @id @default(uuid()) @db.Uuid
  projeto_id      String           @db.Uuid
  fase_id         String?          @db.Uuid
  titulo          String
  descricao       String?          @db.Text
  status          StatusPendencia
  prazo           DateTime?        @db.Date
  data_resolucao  DateTime?        @db.Date
  created_at      DateTime         @default(now())
  updated_at      DateTime         @updatedAt

  projeto         Projeto          @relation(fields: [projeto_id], references: [id], onDelete: Cascade)
  fase            Fase?            @relation(fields: [fase_id], references: [id], onDelete: SetNull)

  @@index([projeto_id])
  @@index([fase_id])
  @@map("pendencias")
}

model Documento {
  id                    String    @id @default(uuid()) @db.Uuid
  projeto_id            String    @db.Uuid
  fase_id               String?   @db.Uuid
  titulo                String
  descricao             String?   @db.Text
  tipo_documento        String?
  url_arquivo_ou_link   String
  data_publicacao       DateTime? @db.Date
  created_at            DateTime  @default(now())
  updated_at            DateTime  @updatedAt

  projeto               Projeto   @relation(fields: [projeto_id], references: [id], onDelete: Cascade)
  fase                  Fase?     @relation(fields: [fase_id], references: [id], onDelete: SetNull)

  @@index([projeto_id])
  @@index([fase_id])
  @@map("documentos")
}

model MudancaDirecao {
  id                  String    @id @default(uuid()) @db.Uuid
  projeto_id          String    @db.Uuid
  fase_id             String?   @db.Uuid
  titulo              String
  descricao           String    @db.Text
  motivo              String?   @db.Text
  impacto_em_prazo    String?   @db.Text
  impacto_em_escopo   String?   @db.Text
  data_mudanca        DateTime  @db.Date
  created_at          DateTime  @default(now())
  updated_at          DateTime  @updatedAt

  projeto             Projeto   @relation(fields: [projeto_id], references: [id], onDelete: Cascade)
  fase                Fase?     @relation(fields: [fase_id], references: [id], onDelete: SetNull)

  @@index([projeto_id])
  @@index([fase_id])
  @@map("mudancas_direcao")
}

model EventoTimeline {
  id              String               @id @default(uuid()) @db.Uuid
  projeto_id      String               @db.Uuid
  fase_id         String?              @db.Uuid
  tipo_evento     TipoEventoTimeline
  titulo          String
  descricao       String?              @db.Text
  data_evento     DateTime             @db.Date
  origem_tipo     String?
  origem_id       String?              @db.Uuid
  created_at      DateTime             @default(now())
  updated_at      DateTime             @updatedAt

  projeto         Projeto              @relation(fields: [projeto_id], references: [id], onDelete: Cascade)
  fase            Fase?                @relation(fields: [fase_id], references: [id], onDelete: SetNull)

  @@index([projeto_id])
  @@index([fase_id])
  @@index([origem_id])
  @@map("eventos_timeline")
}
```

#### 4. Create `.env.local.example`

**File**: `.env.local.example`

```env
# Supabase — PostgreSQL connection
DATABASE_URL="postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres"

# Supabase — Auth
NEXT_PUBLIC_SUPABASE_URL="https://[ref].supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="[anon-key]"
SUPABASE_SERVICE_ROLE_KEY="[service-role-key]"
```

### Success Criteria:

#### Automated Verification:
- [x] Dependencias instaladas sem erros: `pnpm add` completa
- [x] Prisma client gerado: `npx prisma generate`
- [x] Migration aplicada: `npx prisma migrate dev --name init`
- [x] Build passa: `pnpm build`

#### Manual Verification:
- [ ] `npx prisma studio` exibe todas as 12 tabelas com campos e relacoes corretos
- [x] `.env.local` configurado com as credenciais reais do Supabase (pre-requisito para migration)

**Implementation Note**: Antes de rodar `prisma migrate dev`, o usuario precisa ter o projeto Supabase criado e o `.env.local` configurado com as credenciais reais. Pausar aqui para confirmacao manual.

---

## Phase 2: Infrastructure — lib/

### Overview

Criar os 3 arquivos de infraestrutura: Prisma singleton (`db.ts`), Supabase client factories (`supabase.ts`), e `requireAuth()` (`auth.ts`).

### Changes Required:

#### 1. Prisma singleton

**File**: `src/lib/db.ts`

```ts
import 'server-only'
import { PrismaClient } from '@prisma/client'

const globalForPrisma = globalThis as unknown as { prisma: PrismaClient }

export const db =
  globalForPrisma.prisma ??
  new PrismaClient({
    log: process.env.NODE_ENV === 'development' ? ['query'] : [],
  })

if (process.env.NODE_ENV !== 'production') globalForPrisma.prisma = db
```

#### 2. Supabase client factories

**File**: `src/lib/supabase.ts`

Duas factories:
- `createServerSupabaseClient()` — Server Components, Server Actions, Route Handlers
- `createBrowserSupabaseClient()` — Client Components

```ts
import 'server-only'
import { createServerClient } from '@supabase/ssr'
import { cookies } from 'next/headers'

export async function createServerSupabaseClient() {
  const cookieStore = await cookies()

  return createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll: () => cookieStore.getAll(),
        setAll: (cookiesToSet) => {
          try {
            cookiesToSet.forEach(({ name, value, options }) =>
              cookieStore.set(name, value, options)
            )
          } catch {
            // setAll falha em Server Components (read-only cookies)
            // Seguro ignorar — o cookie sera setado pelo Route Handler ou middleware
          }
        },
      },
    }
  )
}
```

**File**: `src/lib/supabase-browser.ts` (separado — sem `server-only`)

```ts
import { createBrowserClient } from '@supabase/ssr'

export function createBrowserSupabaseClient() {
  return createBrowserClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
  )
}
```

> **Nota**: `createBrowserSupabaseClient` esta em arquivo separado porque nao pode ter `import 'server-only'`. O PRD-00b diz "dois exports no mesmo arquivo", mas `server-only` impediria o import em Client Components. Separar em dois arquivos e a solucao correta.

#### 3. requireAuth()

**File**: `src/lib/auth.ts`

```ts
import 'server-only'
import { cache } from 'react'
import { createServerClient } from '@supabase/ssr'
import { cookies } from 'next/headers'
import { redirect } from 'next/navigation'

export const requireAuth = cache(async () => {
  const cookieStore = await cookies()

  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll: () => cookieStore.getAll(),
      },
    }
  )

  const {
    data: { user },
  } = await supabase.auth.getUser()

  if (!user) redirect('/login')

  return user
})
```

Conforme spec: `requireAuth` cria seu proprio Supabase client internamente (nao reutiliza `lib/supabase.ts`) para ter controle total sobre `cookies()` e deduplicacao via `React.cache()`.

### Success Criteria:

#### Automated Verification:
- [x] Build passa: `pnpm build`
- [x] Nenhum erro de TypeScript nos 3 arquivos

#### Manual Verification:
- [x] N/A — estes arquivos sao infraestrutura sem UI. Verificacao real sera na Phase 3.

---

## Phase 3: Routes & Auth

### Overview

Criar `proxy.ts` com redirect auth-aware para `/`, protecao de rotas internas, OAuth callback, pagina de login, layout autenticado com SessionProvider, e dashboard placeholder. Deletar `app/page.tsx`.

### Changes Required:

#### 1. proxy.ts — protecao de rotas

**File**: `src/proxy.ts`

```ts
import { NextResponse } from 'next/server'
import type { NextRequest } from 'next/server'

const internalPrefixes = ['/dashboard', '/projetos', '/clientes']

export function proxy(request: NextRequest) {
  const { pathname } = request.nextUrl

  // Redirect auth-aware para /
  if (pathname === '/') {
    const session = request.cookies.get('sb-access-token')
    return NextResponse.redirect(
      new URL(session ? '/dashboard' : '/login', request.url)
    )
  }

  // Protecao de rotas internas
  const isInternalRoute = internalPrefixes.some((p) => pathname.startsWith(p))
  if (isInternalRoute) {
    const session = request.cookies.get('sb-access-token')
    if (!session) {
      return NextResponse.redirect(new URL('/login', request.url))
    }
  }

  return NextResponse.next()
}

export const config = {
  matcher: [
    '/((?!_next/static|_next/image|favicon.ico|sitemap.xml|robots.txt).*)',
  ],
}
```

**Diferenca do codigo em `04_nextjs.md`**: adicionado bloco de redirect auth-aware para `/` conforme `05_urls.md`.

#### 2. Delete app/page.tsx

**File**: `src/app/page.tsx`
**Action**: Deletar o arquivo

Conforme `05_urls.md`: "/" redireciona via `proxy.ts`, sem `page.tsx`. O proxy intercepta antes do roteador do Next.js, entao o arquivo e desnecessario.

#### 3. OAuth callback Route Handler

**File**: `src/app/auth/callback/route.ts`

```ts
import { createServerClient } from '@supabase/ssr'
import { cookies } from 'next/headers'
import { NextResponse, type NextRequest } from 'next/server'

export async function GET(request: NextRequest) {
  const { searchParams, origin } = new URL(request.url)
  const code = searchParams.get('code')
  const next = searchParams.get('next') ?? '/dashboard'

  if (code) {
    const cookieStore = await cookies()
    const supabase = createServerClient(
      process.env.NEXT_PUBLIC_SUPABASE_URL!,
      process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
      {
        cookies: {
          getAll: () => cookieStore.getAll(),
          setAll: (cookiesToSet) => {
            cookiesToSet.forEach(({ name, value, options }) =>
              cookieStore.set(name, value, options)
            )
          },
        },
      }
    )
    const { error } = await supabase.auth.exchangeCodeForSession(code)
    if (!error) return NextResponse.redirect(`${origin}${next}`)
  }

  return NextResponse.redirect(`${origin}/login?error=auth_callback_failed`)
}
```

#### 4. Login page

**File**: `src/app/(auth)/login/page.tsx` (Server Component)

```tsx
import { LoginButton } from './login-button'

export default async function LoginPage(props: PageProps<'/login'>) {
  const searchParams = await props.searchParams
  const error = searchParams?.error

  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-6 p-8">
      <div className="flex flex-col items-center gap-2 text-center">
        <h1 className="text-3xl font-bold">Gestao de Projetos</h1>
        <p className="text-muted-foreground">
          Faca login para acessar o painel
        </p>
      </div>

      <LoginButton />

      {error && (
        <p className="text-sm text-destructive">
          Erro ao fazer login. Tente novamente.
        </p>
      )}
    </main>
  )
}
```

**File**: `src/app/(auth)/login/login-button.tsx` (Client Component)

```tsx
'use client'

import { Button } from '@/components/ui/button'
import { createBrowserSupabaseClient } from '@/lib/supabase-browser'

export function LoginButton() {
  async function handleLogin() {
    const supabase = createBrowserSupabaseClient()
    await supabase.auth.signInWithOAuth({
      provider: 'google',
      options: {
        redirectTo: `${window.location.origin}/auth/callback`,
      },
    })
  }

  return (
    <Button onClick={handleLogin} size="lg">
      Entrar com Google
    </Button>
  )
}
```

#### 5. SessionProvider

**File**: `src/components/providers/session-provider.tsx`

```tsx
'use client'

import { createContext, useContext } from 'react'
import type { User } from '@supabase/supabase-js'

const SessionContext = createContext<User | null>(null)

export function SessionProvider({
  children,
  user,
}: {
  children: React.ReactNode
  user: User
}) {
  return (
    <SessionContext.Provider value={user}>{children}</SessionContext.Provider>
  )
}

export function useSession() {
  const user = useContext(SessionContext)
  if (!user) {
    throw new Error('useSession must be used within a SessionProvider')
  }
  return user
}
```

#### 6. Internal layout (autenticado)

**File**: `src/app/(internal)/layout.tsx`

```tsx
import { requireAuth } from '@/lib/auth'
import { SessionProvider } from '@/components/providers/session-provider'

export default async function InternalLayout({
  children,
}: {
  children: React.ReactNode
}) {
  const user = await requireAuth()

  return <SessionProvider user={user}>{children}</SessionProvider>
}
```

#### 7. Dashboard placeholder

**File**: `src/app/(internal)/dashboard/page.tsx`

```tsx
import { requireAuth } from '@/lib/auth'

export default async function DashboardPage() {
  const user = await requireAuth()

  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-4 p-8">
      <h1 className="text-2xl font-bold">Dashboard</h1>
      <p className="text-muted-foreground">
        Autenticado como {user.email}
      </p>
    </main>
  )
}
```

> Dashboard chama `requireAuth()` diretamente em vez de usar `useSession()` — e um Server Component e nao precisa de contexto client-side. O `requireAuth()` e deduplicado via `React.cache()`, entao layout + page = 1 chamada.

### Success Criteria:

#### Automated Verification:
- [x] Build passa: `pnpm build`
- [x] Nenhum erro de TypeScript
- [x] `proxy.ts` exporta `proxy()` e `config` (verificavel no build)

#### Manual Verification:
- [x] Acessar `/dashboard` sem sessao → redirecionado para `/login`
- [x] Acessar `/login` → pagina com botao "Entrar com Google" visivel
- [x] Clicar "Entrar com Google" → redirecionado para fluxo OAuth do Google
- [x] Completar fluxo OAuth → redirecionado para `/dashboard` com email visivel
- [x] Acessar `/` autenticado → redirecionado para `/dashboard`
- [x] Acessar `/` sem sessao → redirecionado para `/login`

**Implementation Note**: O fluxo OAuth requer projeto Supabase com Google OAuth configurado (Authentication > Providers > Google). Configurar `Redirect URL = http://localhost:3000/auth/callback` no Supabase dashboard antes de testar.

---

## Testing Strategy

### Pre-requisites (usuario configura manualmente):
1. Projeto Supabase criado
2. `.env.local` com credenciais reais (copiar de `.env.local.example`)
3. Google OAuth provider habilitado no Supabase dashboard
4. Redirect URL configurado: `http://localhost:3000/auth/callback`

### Manual Testing Steps:
1. `pnpm dev` — servidor inicia sem erros
2. Acessar `http://localhost:3000/` — redireciona para `/login`
3. Clicar "Entrar com Google" — inicia fluxo OAuth
4. Completar login — redireciona para `/dashboard` com email visivel
5. Acessar `http://localhost:3000/` novamente — redireciona para `/dashboard` (autenticado)
6. Acessar `/projetos` sem sessao (em aba anonima) — redireciona para `/login`
7. `npx prisma studio` — 12 tabelas visiveis com campos corretos

### Automated Testing:
```bash
npx prisma generate           # client gerado
npx prisma migrate dev        # migration aplicada
pnpm build                    # zero erros TypeScript
```

## Migration Notes

- Este e o schema inicial — nao ha dados existentes para migrar
- Migration name: `init`
- Se precisar recriar: `npx prisma migrate reset` (apaga tudo e recria)

## References

- Original PRD: `specs/prds/prd-00b-auth-db.md`
- Research: `specs/workflow/research/2026-03-12-prd-00b-auth-db.md`
- Foundation specs:
  - `specs/foundation/02_dominio.md` — entidades e regras
  - `specs/foundation/03_arquitetura.md` — stack, versoes, camadas
  - `specs/foundation/04_nextjs.md` — proxy.ts, auth, db, callback patterns
  - `specs/foundation/05_urls.md` — mapa de rotas (source of truth para routing)
